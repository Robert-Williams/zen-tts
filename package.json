{
  "name": "tts-server",
  "version": "1.0.0",
  "description": "service to generate audio file from text requests",
  "main": "server.mjs",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Robert Williams",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
